{
  "schemaVersion": 4,
  "kind": "main-finance-runtime-recovery-v4-target-redeploy-postflight-contract",
  "environment": "staging",
  "endpoint": "https://bljeoovhydhjhdzwplxh.supabase.co/functions/v1/finance-manage-access-v2",
  "method": "POST",
  "action": "attest",
  "ambientAuthorizationAllowed": false,
  "expectedStatus": 200,
  "expectedContentType": "application/json",
  "expectedResponseKeys": [
    "ok",
    "action",
    "provided_descriptor_replayed",
    "database_clock",
    "checked_count",
    "mismatch_count",
    "state_sha256",
    "attested_at",
    "attestation_proof"
  ],
  "expectedConstants": {
    "ok": true,
    "action": "attest",
    "provided_descriptor_replayed": true,
    "mismatch_count": 0
  },
  "minimumCheckedCount": 1,
  "snapshotSandwich": {
    "clockOrder": "deploy-result D0 database_clock < deploy-result verified attested_at < deploy-result D1 database_clock; then distinct completion D0 database_clock < completion verified attested_at < completion D1 database_clock",
    "stableBindings": [
      "source commit and tree",
      "release manifest and exact successor deployment closure",
      "catalog, descriptor and state hashes",
      "gate phase inventory and privacy inventory hashes",
      "complete sorted desired rows and checked_count",
      "privately branded exact D0 and D1 builder results",
      "two stable Management API multipart reads contain exactly the four manifest-pinned authored files plus exact deployment metadata bound to target id and version",
      "hostedSourceClosureSha256 is source identity while bounded size values, multipart MIME labels and optional Content-Length are transport observations only",
      "deploy-result and completion sandwiches are fresh and distinct"
    ],
    "fullHostedInventory": [
      "the exact four ordered source-bound Main values equal the successor private bundle",
      "the two inherited generated values remain proof-only and are never re-set",
      "all Main secret names and value digests outside the exact mutation set remain unchanged",
      "only updated_at may differ for the exact seven named SUPABASE rows",
      "every Finance secret row remains exactly unchanged",
      "complete Main and Finance inventory SHA-256 values are identical across each D0 and D1 sandwich",
      "the complete 13-row Function inventory changes only by the exact finance-manage-access-v2 target replacement with target version exactly plus one"
    ],
    "functionInventoryPhases": {
      "importedBaseline": "the exact immutable predecessor installed 13-row 0efefe inventory, including the already-present broken target deployment",
      "requiredCanonicalRowKeys": [
        "id",
        "name",
        "slug",
        "ezbr_sha256",
        "entrypoint_path",
        "status",
        "verify_jwt",
        "version",
        "created_at",
        "updated_at"
      ],
      "rowIdentityRules": "id is a canonical UUID, name equals slug, ezbr_sha256 is lowercase SHA-256, entrypoint_path is an absolute file URL ending in .ts, timestamps are positive ordered safe integers, and every additional CLI field is preserved exactly",
      "allowedDispositions": [
        "exact-target-replacement-plus-one"
      ],
      "stability": "each fresh plan and each D0/proof/D1 boundary uses complete stable read rounds bound to its own stage",
      "replacement": "only finance-manage-access-v2 may change; its version is predecessor-relative plus one and its replacement source fields are bound to the exact successor deployment closure, while every non-target row remains byte-exact",
      "divergence": "unchanged target after a claimed deploy, any non-target change, target plus two, missing or extra row, wrong closure, or mixed inventory is terminal",
      "functionDeployAuthorized": true,
      "functionDeployTarget": "finance-manage-access-v2"
    }
  },
  "authority": {
    "completionCause": "verified exact four-name secrets-set stage followed by verified exact target-only function-deploy stage; unknown secrets may resolve only by read-only state_satisfied reconciliation and unknown deploy may resolve only by read-only applied reconciliation; neither retries",
    "hostedMutationCount": 2,
    "functionDeployCount": 1,
    "freshPlanCount": 2,
    "ownerApprovalCount": 2,
    "deployResultSandwichRequired": true,
    "completionSandwichDistinct": true,
    "automaticRetryAllowed": false
  },
  "requiredBindings": [
    "database_clock equals supplied snapshot database_clock",
    "checked_count equals supplied snapshot checked_count",
    "state_sha256 equals supplied snapshot state_sha256",
    "attestation_proof verifies with the inherited v2 operator secret"
  ],
  "receiptRedactions": [
    "attestation_proof",
    "operator request signature",
    "operator secret",
    "trigger secret",
    "privacy secret",
    "subject digest",
    "telegram id"
  ]
}
